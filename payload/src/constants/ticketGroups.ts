// The tabs above the price cards. A fixed list so the same group is not typed
// three different ways across conferences — add a line here to allow a new one.
export const TICKET_GROUPS = ['In-person', 'Remote', 'Combo'];
