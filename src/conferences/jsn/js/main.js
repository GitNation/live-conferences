// Conference-specific entry point.
// Import only the components this conference needs — unlike src/app.js,
// this bundle is built for jsn alone.

import '../../../components/typewriter';
import '../../../components/heroVideo';
